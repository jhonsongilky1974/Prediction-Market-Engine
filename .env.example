# Copiar a `.env` (o exportar en el shell) solo si se quiere probar The Odds API.
# Nunca se hardcodea ni se inventa: si esta variable no está presente, el
# conector src/connectors/odds_api.py se marca NOT_CONFIGURED y el resto del
# pipeline sigue funcionando con normalidad.
ODDS_API_KEY=
